const { createClient } = require('@supabase/supabase-js');
// wait, I don't have supabase-js installed or the env vars directly available here?
// Can I just grep the HTML for the supabase url and key?
